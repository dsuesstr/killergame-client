/// <reference path='../../min.references.ts'/>

module Models {
    export interface IListParams {
        Limit:number;
        Offset:number;
        SortColumn:string;
        SortDirection:string;
    }
}